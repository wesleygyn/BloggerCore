@using Kendo.Mvc.UI
@model BloggerCore.Models.Postagem

@removeTagHelper "*, Microsoft.AspNet.Mvc.Razor"
@removeTagHelper "*, Microsoft.AspNetCore.Mvc.Razor"

@{
    ViewBag.Title = "Nova Postagem";
}

<div class="row">

    <div class="col-md-2 col-lg-2"> </div>
    <div class="col-md-10 col-lg-8">

        <form asp-action="Create" id="Postagens">

            <div class="k-d-flex justify-content-start">
                @(Html.Kendo().TextBox()
                    .Name("Titulo")
                    .Placeholder("Informe o título")
                    .HtmlAttributes(new { style = "width: 100%" })
                )
            </div>

            <br />

            <div id="example" class="demo-section k-content wide editor">
                @(Html.Kendo().Editor()
                    .Name("Descricao")
                    .HtmlAttributes(new { style = "width: 100%; height:840px" })
                    .StyleSheets(css => css
                    .Add(Url.Content("~/shared/web/editor/editorStyles.css"))
                    )
                    .Tools(tools => tools
                    .Clear()
                    .Bold().Italic().Underline()
                    .JustifyLeft().JustifyCenter().JustifyRight()
                    .InsertUnorderedList().InsertOrderedList()
                    .Outdent().Indent()
                    .CreateLink().Unlink()
                    .InsertImage()
                    .TableEditing()
                    .FontName()
                    .FontSize()
                    .ForeColor().BackColor()
                    )
                    .ImageBrowser(imageBrowser => imageBrowser
                    .Image("~/shared/UserFiles/Images/{0}")
                    .Read("Read", "ImageBrowser")
                    .Create("Create", "ImageBrowser")
                    .Destroy("Destroy", "ImageBrowser")
                    .Upload("Upload", "ImageBrowser")
                    )
                    .Value(@<text>
                        <p>

                        </p>
                    </text>)
                )
            </div>

            <br />

            <div class="k-d-flex justify-content-start">
                <input type="submit" value="Salvar" class="btn btn-outline-dark" /> |
                <a class="btn btn-outline-dark" asp-controller="Home" asp-action="Index">Voltar</a>
            </div>

        </form>

    </div>

</div>